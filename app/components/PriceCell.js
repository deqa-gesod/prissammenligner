// Én pris med farge etter status. Grønn (mint) ved billigst, ellers nøytral.
// Implementeres i Fase 7. Se /mockup/a for foreløpig design.
function PriceCell({ price, isCheapest, campaign_price, campaign_text }) {
  return (
    <div className="rounded-lg py-2 px-3 text-center tabular-nums">
      TODO: PriceCell ({price})
    </div>
  )
}

export default PriceCell
