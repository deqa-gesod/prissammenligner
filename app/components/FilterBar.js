// Filter-chips + sort-dropdown over produktlisten.
// Implementeres i Fase 7. Se /mockup/a eller /mockup/b for foreløpig design.
function FilterBar({ filters, onChange }) {
  return (
    <div className="mb-8 flex flex-wrap items-center gap-2 text-sm">
      TODO: FilterBar
    </div>
  )
}

export default FilterBar
