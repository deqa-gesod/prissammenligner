// Tabell-rad: én hel produkt-visning på tvers av alle butikker.
// Implementeres i Fase 7 basert på valgt mockup. Se /mockup/a for foreløpig design.
function ProductRow({ name, image, size, unit, category, prices }) {
  return (
    <div className="grid grid-cols-1 sm:grid-cols-[2fr_1fr_1fr_1fr_1fr] gap-3 px-6 py-5 border-b border-rose-mist items-center">
      TODO: ProductRow ({name})
    </div>
  )
}

export default ProductRow
